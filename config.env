# =============================================================================
# Air Quality Sensors - Personal Network Configuration
# =============================================================================
# This file contains YOUR specific network settings. It is tracked in git
# initially so you can sync it to your machine, but should be gitignored
# afterward to keep your personal info private.
#
# INSTRUCTIONS:
# 1. After you `git pull` this file to your local machine, edit the values
#    below to match YOUR network setup.
# 2. Then uncomment the config.env line in .gitignore to stop tracking it.
# 3. Your personal config stays local, the repo stays clean.
# =============================================================================

# -----------------------------------------------------------------------------
# Network Addresses
# -----------------------------------------------------------------------------
# Home Assistant / Raspberry Pi
HOME_ASSISTANT_IP=192.168.68.116
HOME_ASSISTANT_HOSTNAME=scylla
HOME_ASSISTANT_USER=demeter

# Odroid C1+ (Sensor Collection)
ODROID_IP=192.168.68.109
ODROID_HOSTNAME=odroid
ODROID_USER=cerberus

# Alternative Odroid IP (if using DHCP)
ODROID_ALT_IP=192.168.68.117

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------
# Your local network CIDR (for trusted networks in Home Assistant)
LOCAL_NETWORK_CIDR=192.168.68.0/24

# MQTT Broker (usually same as Home Assistant)
MQTT_BROKER_IP=192.168.68.116
MQTT_PORT=1883

# -----------------------------------------------------------------------------
# Quick Reference Commands (with your IPs)
# -----------------------------------------------------------------------------
# SSH to Home Assistant Pi:
#   ssh demeter@192.168.68.116
#
# SSH to Odroid:
#   ssh cerberus@192.168.68.109
#
# Test MQTT connection:
#   mosquitto_sub -h 192.168.68.116 -t "utsensing/#" -v
#
# Check sensor data flowing:
#   mosquitto_sub -h 192.168.68.116 -t "utsensing/#" -v -C 5
